// W4 run-lir: odd shape (1x3 @ 3x1) to stress stride math. Expected output
// is a single-element tensor [0.20] under the deterministic fill.
const int M = 1;
const int K = 3;
const int N = 1;

void matmul_odd(tensor<f32>[M, K] A, tensor<f32>[K, N] B) {
    tensor<f32>[M, N] C = @matmul(A, B);
    return;
}

int main() { return 0; }
