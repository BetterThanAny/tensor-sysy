// W4 run-lir: rmsnorm along innermost dim on a 1x4 tensor.
const int M = 1;
const int N = 4;

void rmsnorm_layer(tensor<f32>[M, N] X) {
    tensor<f32>[M, N] Y = @rmsnorm(X);
    return;
}

int main() { return 0; }
