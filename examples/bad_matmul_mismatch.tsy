// W3 diagnostic: matmul inner dims disagree (K vs N).
// Verifier should report an error on line 8.
const int M = 4;
const int K = 8;
const int N = 16;

void layer(tensor<f32>[M, K] A, tensor<f32>[N, K] B) {
    tensor<f32>[M, N] C = @matmul(A, B);
    return;
}

int main() { return 0; }
